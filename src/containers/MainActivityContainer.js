import React from 'react';

import ClockContainer from './ClockContainer';

var MainActivityContainer = React.createClass({
  render: function() {
    return (
      <ClockContainer />
    );
  }
});

export default MainActivityContainer;
