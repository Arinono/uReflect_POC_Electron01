import React from 'react';

var ClockFrame = React.createClass({
  render: function() {
    return (
      <div className="clock-frame">
        <div className="clock-center"></div>
      </div>
    );
  }
});

export default ClockFrame;
